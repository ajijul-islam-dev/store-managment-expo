import { StyleSheet, Text, View } from 'react-native';

const history = () => {
  return (
    <View>
      <Text>FirstTab</Text>
    </View>
  );
};

export default history;

const styles = StyleSheet.create({});